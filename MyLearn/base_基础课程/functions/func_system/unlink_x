失败返回-1，错误信息存在error中
用这个函数来删除文件，要拥有这个文件所属目录的写和执行权限

如果是符号链接，删除符号链接
如果是硬链接，硬链接计数-1，当减为0时，释放数据块的inode

如果硬链接为0，有进程已打开文件，并持有文件描述符，则等该进程关闭才删掉
	--可以用来制作一个临时文件：先打新建文件再unlink(只是删除目录项，使之具备被删除的条件)，中途干别的事，进程放弃文件使用权的时候就会自动删掉
